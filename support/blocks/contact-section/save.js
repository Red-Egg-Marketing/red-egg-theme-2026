/**
 * Contact Section Block – Save Component
 *
 * Returns null — this is a dynamic block with a PHP
 * render_callback that handles form shortcode processing.
 */

const SaveContactSection = () => {
    return null;
};

export default SaveContactSection;
